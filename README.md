# Analysing Spotify DataSet

# Overview

## Database 
Data from the SQLite database file "spotify_database.db" is used and analysed. This database contains the following tabular datasets:
- "artist_data": data provided by Spotify containing information collected in the year 2023. The main variables of the dataset include: Artist names, popualarity score (scaled 0-100), number of followers, Genres associated with artist (6 columns).
- "albums_data": tracks are listed by track_id and information is given on the album they appear on.
- "tracks_data": contains information on tracks including id, track_popularity, and whether the track is marked as explicit.
- "features_data": contains features about each track, indexed by id.

## Tools used
Python was used to analyse the data set and the following packages were installed:
- pandas for data wrangling
- numpy for numerical computations
- matplotlib for data visualtions
- sqlite3 to connect the database


# PART 1
## Basic Data Inspection
Contained in the script "basic_data_inspection.py". Basic inspection of the data was performed including:
- How the dataset is structured
- Data types contained in each column
- Computing the number of unique artists
- Ranking top 10 artists by popularity (0-100)
- Ranking top 10 artists by number of followers

Figures (.png) generated by this file:
- "Top_Followers_bar_chart.png"
- "Top_Popularity_bar_chart.png"

## Genres Inspection
Contained in the script "Genres_inspection.py". This file does an analysis based on genres including:
- A function that returns the top 10 artists for a given genre
- Adding a column to the dataset to show the number of genres an artist is associated with
  
- Finding potential correlations between number of genres per artist and their popularity/number of followers
- Moderate correlation found between genres per artist and artist popularity
- Weak correlation found between genre per artist and followers
  
- Adding a column to the dataset for the log(followers)+1, to avoid log(0) issues
- Strong correlation found between genre per artist and the log of followers

Figures (.png) generated by this file:
- "Distribution_genres_bar_chart.png"
-  Around half of the artist have no genres associated
- "Genres_vs_Popularity_boxplot.png"
- "Genres_vs_followers_boxplot.png"

## Popularity



# Part 3

## Investigating eras
Contained in the script "eras.py". This file uses "albums_data" from the database to:
- Investigate the "albums_data" table
- Delete uncessary columns from the original database that contain only empty rows
- Create a new column "decades" to store the decade each song is released in
